
<div class= 'container'>
<h1 class='logo'>Listing posts</h1>

<table class="table table-bordered">
  <tr>
    <th>Title</th>
    <th>Content</th>
    <th>number of comments</th>
    <th>Actions</th>
  
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.title %></td>
	<td><%= post.content %></td>
	<td><%= post.comments.count %></td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
	
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path, :class =>"btn btn-embossed btn-primary" %>
</div>